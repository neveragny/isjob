!!!
%html
  %head
    %title Getajob
    %meta{:charset => "utf-8"}
    %meta{"http-equiv" => "X-UA-Compatible", :content => "IE=edge,chrome=1"}
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1, maximum-scale=1"}
    = stylesheet_link_tag :application
    = javascript_include_tag :application
    = csrf_meta_tags
  %body{:class => params[:controller]}
    %div.topbar
      %div.fill
        %div.container
          = link_to 'GetAJob', root_path, :class => 'brand'
          %ul.nav
            %li
              = link_to 'Home', root_path
            %li
              = link_to 'About', '#about'
            %li
              = link_to 'Contacts', '#contacts'
          %div.pull-right
            %ul.nav
            = render 'shared/navigation'
    %div.container
      %div.content
        - flash.each do |name, msg|
          %div.alert-message.warning{'data-alert' => 'alert' }
            = link_to '×', '#', :class => 'close'
            %p
              %strong
                Welcome, this  just a test notice.Cheers
        %div.page-header
          %h1
            Getta Job
            %small Just do get a super job at your location
        %div.row
          %div.span10
            %h5 something following to be here
            = yield
          %div.span4
            %h6 something less relevant
      %footer
        %p getajob.com 2011
      -#%header
      -#  %nav
      -#    %ul.hmenu
      -#      = render 'shared/navigation'
      -#  - flash.each do |name, msg|
      -#    = content_tag :div, msg, :id => "flash_\#{name}" if msg.is_a?(String)
      -##main{:role => "main"}
      -#  = yield
      -#%footer
